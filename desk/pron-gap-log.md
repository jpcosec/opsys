# pron gap log (deskops-on-pron rewrite)

Every pron limitation found during the deskops rewrite, one row per gap, in the
format from `docs/plan-pron-rewrite.md` §5. Fed to pron's inbox at P6 closeout.

Row format: `date | lane | what deskops needed | pron API consulted | gap | repro (cmd/pytest) | proposed pron change`

## Rows

```
2026-09-17 | P1 | `from pron.world import World` at the seam | pron export surface | `pron.world.__init__` exports nothing; `World`/`Store`/`Graph` must be imported from `pron.world.world`/`pron.world.store`/`pron.world.graph` submodules | PYTHONPATH=$PWD python -c "from pron.world import World" -> ImportError: cannot import name 'World' from 'pron.world' | Re-export World, Store, Graph in pron/world/__init__.py (other consumers already import them from the submodules)
2026-09-17 | P1 | seam must resolve `deskops.models:XxxDoc` refs from any cwd | World(root, pythonpath) | `pythonpath` is opaque in pron's docs but is a sys.path *directory* in sldb's resolver; the bare module name ("deskops.models") silently falls back to cwd and fails from a foreign cwd | cd /tmp && PYTHONPATH=$PWD python -c "from deskops.world import get_world; get_world('<repo>').store.model_names()" with pythonpath="deskops.models" loses models; works only with pythonpath=<repo root> | Document pythonpath as a directory, or accept a modules list (e.g. pythonpath_modules=["deskops.models"]) and build sys.path entries from it
2026-09-17 | P1 | `World.refresh_if_stale()` must leave a usable derived graph on this store | World.refresh / refresh_if_stale | Raises kgdb.ingest.typed.TypedIngestError ("unknown relation type 'tagged_as'") because the store has semantic-tag docs but no kgdb-initialized relation types; floods stderr with one line per skipped edge and offers no pron-level actionable message | PYTHONPATH=$PWD python -c "from deskops.world import get_world; get_world('.').refresh_if_stale()" | Pre-check RelationTypeDoc presence in the refresher and raise a clear "store not kgdb-initialized (run kgdb init)" error; collect skipped edges into the returned report instead of stderr; init relation types from World.ensure_ready
2026-09-17 | P1 | distinguish "model already registered" from "model cannot be imported" | Store.register_model | Returns bool False for ANY SLDBModelError, so an import failure is indistinguishable from a duplicate registration; triage of a failing register_model requires calling sldb's add_model directly | PYTHONPATH=$PWD python -c "from deskops.world import get_world; get_world('.').store.register_model('deskops.models:NoSuchDoc')" -> False, same as an existing model | Return a structured result (registered / exists / import-failed) or raise with the underlying SLDBModelError message
2026-09-17 | P1 | declarative document metadata must feed the derived graph | World.graph / kgdb ingest | `__containment__` and `__references__` on document models (plan §4) have no consumers anywhere in pron or kgdb ingest (only kgdb's RelationDoc declares `__references__` today); graph edges for them are not derived | PYTHONPATH=$PWD python -c "from pron.world.world import World; w=World('.'); print(w.store.model_type('TaskDoc').__references__)" (declared but nothing reads it) | kgdb typed ingest: derive reference edges from `__references__`/`__containment__` on any StructuredNLDoc model, or document that only RelationDoc participates
2026-09-17 | P1 | document model rendering/extraction through the seam | pron export surface | pron does not re-export sldb's `extract_model_data`/`render_model_markdown`; deskops imports them from `sldb.runtime.validation` inside the seam only (plan §2 exception 1 — pron's own docs_sync does the same) | grep sldb.runtime.validation deskops/world.py (documented exception) | Re-export both names from pron (e.g. pron.docs.render/extract)
2026-09-17 | P1 | KGDB contract validation of the deskops snapshot artifact | pron export surface | pron does not re-export `kgdb.contracts.io.GraphSnapshot`; deskops imports it inside the seam only (plan §2 exception 2 — contract layer, not document access) | grep kgdb.contracts.io deskops/world.py (documented exception) | Re-export GraphSnapshot from pron (e.g. pron.graph.snapshot)
2026-09-17 | P1 | store format migration landed as collateral of a pron refresh | pron World.refresh / Store.update_index | Running pron's refresh during lane P1 migrated the store documents layout (monolithic .sldb/core/documents/AtomDoc.yaml deleted -> per-document YAML files under .sldb/core/documents/AtomDoc/*.yaml) as a side effect, committed inside the P1 lane commit (3c041ee) | git show 3c041ee --stat (sldb store migration files) | Document that refresh migrates the store document layout, or make the layout migration explicit/opt-in so consumers can scope/plan it
```